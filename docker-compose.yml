services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: app
    image: mondok-qu:latest
    container_name: mondokqu-app
    restart: unless-stopped
    environment:
      APP_ENV: production
      APP_DEBUG: false
      APP_URL: "${APP_URL}"
      APP_KEY: "${APP_KEY}"
      DB_CONNECTION: mysql
      DB_HOST: mariadb
      DB_PORT: 3306
      DB_DATABASE: "${DB_DATABASE}"
      DB_USERNAME: "${DB_USERNAME}"
      DB_PASSWORD: "${DB_PASSWORD}"
      REDIS_CLIENT: phpredis
      REDIS_HOST: redis
      REDIS_PORT: 6379
      SESSION_DRIVER: database
      CACHE_STORE: "${CACHE_STORE:-redis}"
      QUEUE_CONNECTION: redis
      FILESYSTEM_DISK: local
      MAIL_MAILER: "${MAIL_MAILER:-smtp}"
      MAIL_HOST: "${MAIL_HOST}"
      MAIL_PORT: "${MAIL_PORT}"
      MAIL_USERNAME: "${MAIL_USERNAME}"
      MAIL_PASSWORD: "${MAIL_PASSWORD}"
      MAIL_ENCRYPTION: "${MAIL_ENCRYPTION:-tls}"
      MAIL_FROM_ADDRESS: "${MAIL_FROM_ADDRESS}"
      MAIL_FROM_NAME: "${APP_NAME}"
      SENTRY_LARAVEL_DSN: "${SENTRY_LARAVEL_DSN}"
      SENTRY_TRACES_SAMPLE_RATE: 0.2
      SENTRY_ENABLE_LOGS: true
      APP_NAME: "Mondok Qu"
      APP_LOCALE: id
      APP_TIMEZONE: Asia/Jakarta
      LOG_CHANNEL: stack
      LOG_LEVEL: warning
      SESSION_LIFETIME: 120
      SESSION_ENCRYPT: true
      SESSION_SECURE_COOKIE: true
      AUTH_DEFAULT_USER_PASSWORD: "${AUTH_DEFAULT_USER_PASSWORD:-Password123!}"
      SAAS_TRIAL_DAYS: 14
      SAAS_DEFAULT_PLAN: trial
    volumes:
      - mondok-qu-storage:/var/www/html/storage/app
    depends_on:
      mariadb:
        condition: service_healthy
      redis:
        condition: service_started
    networks:
      - mondok-qu-network

  nginx:
    build:
      context: .
      dockerfile: Dockerfile
      target: nginx
    image: mondok-qu-nginx:latest
    container_name: mondokqu-nginx
    restart: unless-stopped
    ports:
      - "${APP_PORT:-80}:80"
      - "443:443"
    volumes:
      - ./docker/nginx/default.conf:/etc/nginx/conf.d/default.conf:ro
      - mondok-qu-storage:/var/www/html/storage/app:ro
    depends_on:
      - app
    networks:
      - mondok-qu-network

  mariadb:
    image: mariadb:11.4
    container_name: mondokqu-mariadb
    restart: unless-stopped
    ports:
      - "127.0.0.1:${DB_PORT_EXTERNAL:-3307}:3306"
    environment:
      MYSQL_DATABASE: "${DB_DATABASE}"
      MYSQL_USER: "${DB_USERNAME}"
      MYSQL_PASSWORD: "${DB_PASSWORD}"
      MYSQL_ROOT_PASSWORD: "${DB_ROOT_PASSWORD}"
    volumes:
      - mondok-qu-db:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mariadb-admin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - mondok-qu-network

  redis:
    image: redis:7-alpine
    container_name: mondokqu-redis
    restart: unless-stopped
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - mondok-qu-redis:/data
    networks:
      - mondok-qu-network

  scheduler:
    image: mondok-qu:latest
    container_name: mondokqu-scheduler
    restart: unless-stopped
    command: php artisan schedule:work
    environment:
      APP_ENV: production
      APP_DEBUG: false
      APP_URL: "${APP_URL}"
      APP_KEY: "${APP_KEY}"
      DB_CONNECTION: mysql
      DB_HOST: mariadb
      DB_PORT: 3306
      DB_DATABASE: "${DB_DATABASE}"
      DB_USERNAME: "${DB_USERNAME}"
      DB_PASSWORD: "${DB_PASSWORD}"
      REDIS_CLIENT: phpredis
      REDIS_HOST: redis
      REDIS_PORT: 6379
      CACHE_STORE: "${CACHE_STORE:-redis}"
      QUEUE_CONNECTION: redis
      MAIL_MAILER: "${MAIL_MAILER:-log}"
      FILESYSTEM_DISK: local
      SENTRY_LARAVEL_DSN: "${SENTRY_LARAVEL_DSN}"
      SENTRY_ENABLE_LOGS: true
      APP_NAME: "Mondok Qu"
      APP_LOCALE: id
      APP_TIMEZONE: Asia/Jakarta
      LOG_CHANNEL: stack
      LOG_LEVEL: warning
      SESSION_LIFETIME: 120
      SESSION_ENCRYPT: true
      SESSION_SECURE_COOKIE: true
      AUTH_DEFAULT_USER_PASSWORD: "${AUTH_DEFAULT_USER_PASSWORD:-Password123!}"
      SAAS_TRIAL_DAYS: 14
      SAAS_DEFAULT_PLAN: trial
    volumes:
      - mondok-qu-storage:/var/www/html/storage/app
    depends_on:
      - mariadb
      - redis
    networks:
      - mondok-qu-network

  queue-worker:
    image: mondok-qu:latest
    container_name: mondokqu-queue
    restart: unless-stopped
    command: php artisan queue:work --sleep=3 --tries=3 --timeout=900
    environment:
      APP_ENV: production
      APP_DEBUG: false
      APP_URL: "${APP_URL}"
      APP_KEY: "${APP_KEY}"
      DB_CONNECTION: mysql
      DB_HOST: mariadb
      DB_PORT: 3306
      DB_DATABASE: "${DB_DATABASE}"
      DB_USERNAME: "${DB_USERNAME}"
      DB_PASSWORD: "${DB_PASSWORD}"
      REDIS_CLIENT: phpredis
      REDIS_HOST: redis
      REDIS_PORT: 6379
      CACHE_STORE: "${CACHE_STORE:-redis}"
      QUEUE_CONNECTION: redis
      MAIL_MAILER: "${MAIL_MAILER:-log}"
      FILESYSTEM_DISK: local
      SENTRY_LARAVEL_DSN: "${SENTRY_LARAVEL_DSN}"
      SENTRY_ENABLE_LOGS: true
      APP_NAME: "Mondok Qu"
      APP_LOCALE: id
      APP_TIMEZONE: Asia/Jakarta
      LOG_CHANNEL: stack
      LOG_LEVEL: warning
      SESSION_LIFETIME: 120
      SESSION_ENCRYPT: true
      SESSION_SECURE_COOKIE: true
      AUTH_DEFAULT_USER_PASSWORD: "${AUTH_DEFAULT_USER_PASSWORD:-Password123!}"
      SAAS_TRIAL_DAYS: 14
      SAAS_DEFAULT_PLAN: trial
    volumes:
      - mondok-qu-storage:/var/www/html/storage/app
    depends_on:
      - mariadb
      - redis
    networks:
      - mondok-qu-network

volumes:
  mondok-qu-db:
  mondok-qu-redis:
  mondok-qu-storage:

networks:
  mondok-qu-network:
    driver: bridge
